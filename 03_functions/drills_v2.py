# 03_functions · Micro‑drills (Beginner Edition)
# Use only Primitive Types, Control Flow, and Functions (no data structures yet unless the prompt shows a list).

# Drill 1: Define and return
# Create a function calculate_area(length, width) → return length*width. Print the result for 5 and 3.


# Drill 2: Default args
# Create make_greeting(name, greeting="Hello") → string. Print result for two different calls.


# Drill 3: Grade function
# get_grade(score) → "A"/"B"/"C"/"D"/"F" based on ranges. Test a few values.


# Drill 4: Safe divide (tuple return)
# safe_divide(a, b) → (result, message). If b==0, (None, "Cannot divide by zero"). Print both.


# Drill 5: Loop inside function
# sum_range(start, end) inclusive using a for loop. Print sum_range(1, 5).


# Drill 6: Input helper (while)
# get_positive_number() asks until user enters > 0, then returns it. Call it once, print the value.


# Drill 7: Default parameters (multiple)
# calculate_interest(P, r=0.05, years=1) → P*(1+r)**years. Print two calls with different args.


# Drill 8: Boolean validator
# validate_email(email) → True/False using membership checks for "@" and "." and length >= 5. Print results for two inputs.


# Drill 9: Global counter
# Use a global variable call_count. tracked_add(a, b) increments it and returns the sum. Call twice and print the counter.


# Drill 10: Nested function / closure
# create_multiplier(factor) returns a function that multiplies input by factor. Create double and triple, test with 4.


# Drill 11: *args
# find_maximum(*args) returns the largest number. Test with 3 values.


# Drill 12: Lambdas + dispatcher
# Implement add, sub, mul, div (safe) as lambdas. create_calculator(op) returns the corresponding lambda or None. Use it.
